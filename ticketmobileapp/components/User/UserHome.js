import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const UserHome = () => {
  return (
    <SafeAreaView>
      <Text>Trang chủ của user</Text>
    </SafeAreaView>
  );
};

export default UserHome;
